
##Customer_master

ALTER TABLE collectors      ADD PRIMARY KEY (collector_id);
ALTER TABLE marketcircles   ADD PRIMARY KEY (header_id), ADD UNIQUE (mc_code);
ALTER TABLE customermasters ADD PRIMARY KEY (header_id);
ALTER TABLE customersites   ADD PRIMARY KEY (line_id);

ALTER TABLE marketcircles ADD FOREIGN KEY (collector_id) REFERENCES collectors (collector_id);
ALTER TABLE customersites ADD FOREIGN KEY (header_id)    REFERENCES customermasters (header_id);

-- no FK on mc_code: 1,063 rows would fail. Index it instead.
CREATE INDEX ON customersites (mc_code);
CREATE INDEX ON customersites (site_use_id);   -- what the fact tables join on
CREATE INDEX ON customersites (customer_id);


